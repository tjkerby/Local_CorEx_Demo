# MNIST Paper Experiments

Use this folder for notebooks, model checkpoints, and helper code that reproduce the MNIST-focused Local CorEx studies. Keep large datasets or trained weights out of git; instead provide download instructions.
